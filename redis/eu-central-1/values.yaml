architecture: standalone

auth:
  enabled: false

image:
  registry: docker.io
  repository: redis
  tag: "8.6.2-alpine"

master:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  persistence:
    enabled: false
